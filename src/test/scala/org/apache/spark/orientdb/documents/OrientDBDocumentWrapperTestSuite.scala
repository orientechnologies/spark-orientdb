package org.apache.spark.orientdb.documents

import org.scalatest.{BeforeAndAfter, FunSuite}
import org.mockito.Mockito._

class OrientDBDocumentWrapperTestSuite extends FunSuite with BeforeAndAfter {
  before {
  }

  after {

  }

  test("orientdb wrapper") {
//    val dbAccess = new MockOrientDBDocument().documentWrapper

//    println(dbAccess.doesClusterExists("test_orient"))
  }
}